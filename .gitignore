    # Ignore Node.js dependencies across the entire project
    node_modules/

    # Ignore environment variable files (CRUCIAL for sensitive data)
    .env

    # Ignore client-side specific build and dependency folders
    client/dist/
    client/node_modules/

    # Ignore server-side specific dependency folders
    server/node_modules/

    # Ignore common OS and editor specific files
    .vscode/
    .DS_Store
    npm-debug.log*
    yarn-debug.log*
    yarn-error.log*